{% extends 'base.html.twig' %}

{% block container %}
    <center>
    <h1>Partenaire creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('partenaire_index') }}">Back to the list</a>
        </li>
    </ul>
    </center>
{% endblock %}
